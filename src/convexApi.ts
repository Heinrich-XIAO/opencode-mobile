// Small re-export wrapper so the app can import the generated Convex `api`
// using a stable, project-local path that works with webpack/react-native-web.
export { api } from '../convex/_generated/api';
